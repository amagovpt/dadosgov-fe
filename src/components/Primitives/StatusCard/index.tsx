"use client"
import { StatusCardProps, StatusCard as StatusCardADS } from '@ama-pt/agora-design-system'

export default function StatusCard(args: StatusCardProps) {
    return (
        <StatusCardADS {...args} />
    )
}
