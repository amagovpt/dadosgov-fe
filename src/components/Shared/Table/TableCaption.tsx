"use client"
import { TableCaption as TableCaptionADS, TableCaptionProps } from '@ama-pt/agora-design-system'

export default function TableCaption(props: TableCaptionProps) {
    return (
        <TableCaptionADS {...props} />
    )
}
