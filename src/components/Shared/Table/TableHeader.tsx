"use client"
import { TableHeader as TableHeaderADS, TableHeaderProps } from '@ama-pt/agora-design-system'

export default function TableHeader(props: TableHeaderProps) {
    return (
        <TableHeaderADS {...props} />
    )
}
