"use client"
import { TableRow as TableRowADS, TableRowProps } from '@ama-pt/agora-design-system'

export default function TableRow(props: TableRowProps) {
    return (
        <TableRowADS {...props} />
    )
}
