"use client"
import { TableFooter as TableFooterADS, TableFooterProps } from '@ama-pt/agora-design-system'

export default function TableFooter(props: TableFooterProps) {
    return (
        <TableFooterADS {...props} />
    )
}
