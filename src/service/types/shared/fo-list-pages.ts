export type FoListPageHero = {
  title: string;
  description: string;
};

export type FoListPageSearch = {
  label: string;
  placeholder: string;
  hint: string;
};

export type FoListPageNoResults = {
  icon: string;
  title: string;
  subtitle: string;
  description: string;
};
